# Top-level ESP-IDF project file. Build with `idf.py build` after
# `. $IDF_PATH/export.sh` (ESP-IDF v5.5).
cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(esp32-mcu-telemetry)
