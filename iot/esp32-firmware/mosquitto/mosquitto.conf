# Mosquitto 2.x - minimal demo broker.
# MQTT 5 is supported automatically; the device connects with protocol v5
# to set a Last Will and (optionally) carry trace context in user properties.
listener 1883
allow_anonymous true

log_type all
log_dest stdout
