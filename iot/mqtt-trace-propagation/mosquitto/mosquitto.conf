# Mosquitto 2.x — minimal demo broker.
# MQTT 5 is supported automatically; clients connect with protocol v5 to
# get user properties (the carrier for W3C trace context in this example).
listener 1883
allow_anonymous true

# Verbose enough to see CONNECT / PUBLISH / SUBSCRIBE during the demo.
log_type all
log_dest stdout
