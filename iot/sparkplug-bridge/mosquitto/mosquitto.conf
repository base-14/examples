# Mosquitto 2.x — minimal demo broker (same shape as the Phase 1 example).
# Sparkplug B runs over plain MQTT; the simulator connects with MQTT 5 and
# sets a Last Will so the broker publishes NDEATH when it drops.
listener 1883
allow_anonymous true

log_type all
log_dest stdout
